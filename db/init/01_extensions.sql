-- Enable required extensions
CREATE EXTENSION IF NOT EXISTS timescaledb;
-- Optional but useful for advanced aggregates; not strictly required by our CAggs
CREATE EXTENSION IF NOT EXISTS timescaledb_toolkit;
